if (interactive()) {
  
  suppressMessages({
    library(showtext)
    library(sysfonts)
  })
  
  sysfonts::font_add(
    family = "noto_tc",
    regular = "font/NotoSansTC-Regular.ttf"
  )
  
  showtext::showtext_auto()
  
}
source("private.R")
